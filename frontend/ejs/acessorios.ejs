<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Acessórios - RS Card Store</title>

  <link rel="preconnect" href="https://fonts.googleapis.com"/>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700;800&family=Lato:wght@400;700&display=swap" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"/>
  <!-- usa o modelo existente -->
  <link rel="stylesheet" href="../css/index.css"/>
  <link rel="stylesheet" href="../css/acessorios.css">
</head>
<body class="acessorios-page">
  <!-- HEADER -->
  <header class="main-header">
    <div class="container header-content">
      <a href="/" class="logo">RS<span>CardStore</span></a>

      <nav class="main-nav">
        <a href="/magic">Magic: The Gathering</a>
        <a href="/yugioh">Yu-Gi-Oh!</a>
        <a href="/pokemon">Pokémon TCG</a>
        <a href="/acessorios" class="active">Acessórios</a>
        <a href="/promocoes">Promoções</a>
      </nav>

      <div class="header-actions">
        <a href="#" aria-label="Minha Conta"><i class="fa-solid fa-user"></i></a>
        <a href="#" aria-label="Carrinho de Compras"><i class="fa-solid fa-cart-shopping"></i></a>

        <div class="theme-switch-wrapper">
          <label class="theme-switch" for="theme-checkbox">
            <input type="checkbox" id="theme-checkbox"/>
            <div class="slider round">
              <i class="fa-solid fa-moon"></i>
              <i class="fa-solid fa-sun"></i>
            </div>
          </label>
        </div>
      </div>
    </div>
  </header>

  <main>
    <section class="hero" style="--gradient-start: rgba(10,36,99,0.95); --gradient-end: rgba(255,159,28,0.95);">
      <div class="floating-cards" aria-hidden="true">
        <div class="card card-1" style="--r-start:-10deg; --r-end:-14deg;"></div>
        <div class="card card-2" style="--r-start:12deg; --r-end:16deg;"></div>
        <div class="card card-3" style="--r-start:8deg; --r-end:4deg;"></div>
        <div class="card card-4" style="--r-start:-6deg; --r-end:-10deg;"></div>
        <div class="card card-5" style="--r-start:18deg; --r-end:22deg;"></div>
      </div>

      <div class="container hero-content">
        <h1>Acessórios</h1>
        <p>Organize, proteja e eleve sua experiência de jogo com sleeves, deck boxes, playmats e muito mais.</p>
      </div>
    </section>

    <section class="section" id="lista-acessorios">
      <div class="container">
        <div class="product-toolbar">
          <div class="filter-controls">
            <select name="tipo_produto" id="tipo_produto">
              <option value="">Categoria</option>
              <option value="sleeves">Sleeves</option>
              <option value="deckbox">Deck Boxes</option>
              <option value="playmat">Playmats</option>
              <option value="binder">Binders / Pastas</option>
              <option value="storage">Armazenamento</option>
              <option value="acessorios">Outros Acessórios</option>
            </select>

            <select name="marca" id="marca">
              <option value="">Marca</option>
              <option value="dragonshield">Dragon Shield</option>
              <option value="ultrapro">Ultra Pro</option>
              <option value="bcw">BCW</option>
              <option value="kmc">KMC</option>
            </select>
          </div>

          <div class="sort-options">
            <label for="sort">Ordenar por:</label>
            <select name="sort" id="sort">
              <option value="relevancia">Relevância</option>
              <option value="preco-menor">Menor Preço</option>
              <option value="preco-maior">Maior Preço</option>
              <option value="recentes">Mais Recentes</option>
            </select>
          </div>
        </div>

        <div class="products-grid">
          <% if (produtos && produtos.length > 0) { %>
            <% produtos.forEach(produto => { %>
              <div class="product-card">

                <%# --- Badge Dinâmico: Mostra PROMO ou NOVO se aplicável --- %>
                <% if (produto.promocao) { %>
                  <div class="product-badge" style="background-color:#e74c3c;">PROMO</div>
                <% } else if (produto.novo) { %>
                  <div class="product-badge">NOVO</div>
                <% } %>

                <a href="/produto/<%= produto.id %>" class="product-link-wrapper">
                  <div class="product-image-container">
                    <img src="<%= produto.imagem_url %>" alt="<%= produto.nome %>"/>
                  </div>
                  <div>
                    <h4><%= produto.nome %></h4>
                    <%# --- Bloco de Preço Dinâmico com Desconto --- %>
                    <div class="product-price">
                      <% if (produto.desconto_percentual > 0) { %>
                        <% 
                          // Calcula o preço final
                          const precoOriginal = produto.preco;
                          const precoFinal = precoOriginal * (1 - produto.desconto_percentual / 100);
                        %>
                        
                        <%# Preço original riscado %>
                        <span style="text-decoration: line-through; color: #999; font-size: 0.9em;">
                          R$ <%= parseFloat(precoOriginal).toFixed(2).replace('.', ',') %>
                        </span>
                        
                        <%# Preço final com desconto %>
                        <strong style="margin-left: 8px;">
                          R$ <%= parseFloat(precoFinal).toFixed(2).replace('.', ',') %>
                        </strong>

                      <% } else { %>

                        <%# Exibe o preço normal se não houver desconto %>
                        <strong>R$ <%= parseFloat(produto.preco).toFixed(2).replace('.', ',') %></strong>
                        
                      <% } %>
                    </div>
                  </div>
                </a>
                
                <%# --- O botão de adicionar ao carrinho agora é um formulário POST --- %>
                <form action="/carrinho/adicionar/<%= produto.id %>" method="POST" style="display: inline;">
                    <input type="hidden" name="quantidade" value="1">
                    <button type="submit" class="cta-button">Adicionar ao Carrinho</button>
                </form>
              </div>
            <% }) %>
          <% } else { %>
            <p>Nenhum acessório encontrado no momento.</p>
          <% } %>
        </div>

        <nav class="pagination" aria-label="Paginação de acessórios">
          <ul>
            <li><a href="#" class="prev disabled">Anterior</a></li>
            <li><a href="#" class="active">1</a></li>
            <li><a href="#">2</a></li>
            <li><a href="#">3</a></li>
            <li><a href="#" class="next">Próxima</a></li>
          </ul>
        </nav>
      </div>
    </section>
  </main>

  <footer class="main-footer">
    <div class="container">
      <div class="footer-content">
        <div class="footer-column">
          <h4>RS Card Store</h4>
          <ul>
            <li><a href="#">Sobre Nós</a></li>
            <li><a href="#">Políticas da Loja</a></li>
            <li><a href="#">Trabalhe Conosco</a></li>
          </ul>
        </div>
        <div class="footer-column">
          <h4>Ajuda e Suporte</h4>
          <ul>
            <li><a href="#">Como Comprar</a></li>
            <li><a href="#">Acompanhar Pedido</a></li>
            <li><a href="#">Trocas e Devoluções</a></li>
            <li><a href="#">Perguntas Frequentes</a></li>
          </ul>
        </div>
        <div class="footer-column">
          <h4>Formas de Pagamento</h4>
          <p>Aceitamos Cartão de Crédito, Débito e Pix.</p>
        </div>
        <div class="footer-column">
          <h4>Siga-nos</h4>
          <div class="social-icons">
            <a href="#" aria-label="Facebook"><i class="fab fa-facebook-f"></i></a>
            <a href="#" aria-label="Instagram"><i class="fab fa-instagram"></i></a>
            <a href="#" aria-label="Twitter"><i class="fab fa-twitter"></i></a>
          </div>
        </div>
      </div>
    </div>
    <div class="footer-bottom">
      <p>© 2025 RS Card Store | CNPJ: XX.XXX.XXX/0001-XX. Todos os direitos reservados.</p>
    </div>
  </footer>

  <script>
    const themeSwitch = document.getElementById('theme-checkbox');
    const body = document.body;

    const applyTheme = () => {
      const savedTheme = localStorage.getItem('theme');
      if (savedTheme === 'dark') {
        body.classList.add('dark-mode');
        themeSwitch.checked = true;
      } else {
        body.classList.remove('dark-mode');
        themeSwitch.checked = false;
      }
    };

    const handleThemeChange = (event) => {
      if (event.target.checked) {
        body.classList.add('dark-mode');
        localStorage.setItem('theme', 'dark');
      } else {
        body.classList.remove('dark-mode');
        localStorage.setItem('theme', 'light');
      }
    };

    if (themeSwitch) themeSwitch.addEventListener('change', handleThemeChange);
    document.addEventListener('DOMContentLoaded', applyTheme);
  </script>
</body>
</html>