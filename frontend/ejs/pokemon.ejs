<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Magic: The Gathering - RS Card Store</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700;800&family=Lato:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <link rel="stylesheet" href="../css/index.css">
    <link rel="stylesheet" href="../css/pokemon.css">
</head>
<body class="pokemon-page">
    <%- include('partials/header') %> 

    <main>
        <section class="hero">
            <div class="floating-cards">
                <div class="card card-1"></div>
                <div class="card card-2"></div>
                <div class="card card-3"></div>
                <div class="card card-4"></div>
                <div class="card card-5"></div>
            </div>

            <div class="container hero-content"> <h1>Pokémon</h1>
                <p>Venha capturar nossos produtos. Temos que pegar!</p>
        </section>

        <section class="section" id="lista-produtos">
            <div class="container">
                <%- include('partials/filter-toolbar', { 
                    currentFilters: currentFilters, 
                    baseUrl: baseUrl,
                    searchTerm: locals.searchTerm 
                }) %>

                <div class="products-grid">
                    <% if (typeof produtos !== 'undefined' && produtos.length) { %>
                        <% produtos.forEach(p => { %>
                        <div class="product-card">
                            <% if (p.novo) { %>
                            <div class="product-badge" style="background-color: var(--color-orange-vibrant);">NOVO</div>
                            <% } else if (p.promocao) { %>
                            <div class="product-badge" style="background-color: #e74c3c;">PROMO</div>
                            <% } %>

                            <a href="/produto/<%= p.id %>" class="product-link-wrapper">
                            <div class="product-image-container">
                                <img
                                src="<%= p.imagem_url || 'https://via.placeholder.com/400x400?text=Pokémon' %>"
                                alt="<%= p.nome %>">
                            </div>
                            <div>
                                <h4><%= p.nome %></h4>
                                <%# --- ATUALIZAÇÃO: Lógica de desconto adicionada --- %>
                                <div class="product-price">
                                    <% if (p.desconto_percentual > 0) { %>
                                        <% 
                                        const precoOriginal = p.preco;
                                        const precoFinal = precoOriginal * (1 - p.desconto_percentual / 100);
                                        %>
                                        <span style="text-decoration: line-through; color: #999; font-size: 0.9em;">
                                        R$ <%= parseFloat(precoOriginal).toFixed(2).replace('.', ',') %>
                                        </span>
                                        <strong style="margin-left: 8px;">
                                        R$ <%= parseFloat(precoFinal).toFixed(2).replace('.', ',') %>
                                        </strong>
                                    <% } else { %>
                                        <strong>R$ <%= parseFloat(p.preco).toFixed(2).replace('.', ',') %></strong>
                                    <% } %>
                                </div>
                            </div>
                            </a>

                            <form action="/carrinho/adicionar/<%= p.id %>" method="POST" style="display: inline;">
                                <input type="hidden" name="quantidade" value="1">
                                <button type="submit" class="cta-button">Adicionar ao Carrinho</button>
                            </form>
                        </div>
                        <% }) %>
                    <% } else { %>
                        <div class="empty-state">
                        <p>Nenhum produto de Pokémon disponível no momento.</p>
                        </div>
                    <% } %>
                </div>

                <%- include('partials/pagination') %>

            </div>
        </section>
    </main>
    
    <%- include('partials/footer') %>

    <script src="/js/theme.js"></script>

</body>
</html>