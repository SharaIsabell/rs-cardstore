<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redefinir Senha - RS Card Store</title>
    <link rel="stylesheet" href="../css/login.css"> <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700;800&family=Lato:wght@400;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --color-orange-vibrant: #FF9F1C;
        }

        .login-form {
            padding: 2rem 2.5rem;
            display: flex;
            flex-direction: column;
            justify-content: center; 
            align-items: center;  
            height: 100%;         
            text-align: center;    
        }

        .icon-wrapper {
            font-size: 3.5rem; 
            margin-bottom: 1.5rem;
            color: var(--color-orange-vibrant);
        }

        .login-form h2 {
            font-size: 2.2rem;
            margin-bottom: 0.5rem;
        }

        .login-form p {
            font-size: 1rem;
            max-width: 350px;
            margin-bottom: 2rem;
            color: rgba(255, 255, 255, 0.9);
        }

        @media (max-width: 480px) {
            .login-form {
                padding: 2rem 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div id="particles-js"></div>

    <div class="auth-container" style="display: flex; width: 100%; max-width: 540px; min-height: 540px; backdrop-filter: blur(10px);">
        
        <div class="forms-container" style="position: relative; width: 100%; height: 100%;">
            <div class="login-form" style="opacity: 1; z-index: 2; transform: none;">
                
                <div class="icon-wrapper">
                    <span>🔒</span>
                </div>

                <h2>Crie sua nova senha</h2> <p>Escolha uma senha forte com no mínimo 8 caracteres, letras e números.</p> <% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
                    <div class="error-message"><%= errorMessage %></div> <% } %>
                
                <form action="/redefinir-senha" method="POST"> <input type="hidden" name="token" value="<%= token %>"> <div class="input-group"> <input type="password" id="nova_senha" name="nova_senha" required minlength="8" placeholder=" "> <label for="nova_senha">Nova Senha</label> </div>
                    <div class="input-group"> <input type="password" id="confirmar_nova_senha" name="confirmar_nova_senha" required placeholder=" "> <label for="confirmar_nova_senha">Confirmar Nova Senha</label> </div>
                    <button type="submit" class="cta-button">Salvar Nova Senha</button> </form>
            </div>
        </div>
    </div>
    <script src="../js/particles.min.js"></script> <script src="../js/login.js"></script> </body>
</html>